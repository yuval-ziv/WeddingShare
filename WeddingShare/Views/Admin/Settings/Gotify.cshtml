@model IDictionary<string, string>?
@using WeddingShare.Constants
@using WeddingShare.Enums
@using WeddingShare.Extensions

@if (Model != null && Model.Any())
{
    <div class="row mb-4 mb-lg-4">
        <div class="col-12">
            <label>@_localizer["Settings_Gotify_Enabled_Label"].Value</label>
            <select class="setting-field form-control" data-setting-name="@Notifications.Gotify.Enabled">
                @foreach (var option in ViewOptions.YesNo)
                {
                    if (Model.GetValue(Notifications.Gotify.Enabled.ToUpper(), "false").Equals(option.Value, StringComparison.OrdinalIgnoreCase))
                    {
                        <option value="@option.Value" selected="selected">@option.Key</option>
                    }
                    else
                    {
                        <option value="@option.Value">@option.Key</option>
                    }
                }
            </select>
            <div class="form-text">@_localizer["Settings_Gotify_Enabled_Help"].Value</div>
        </div>
    </div>

    <div class="row mb-4 mb-lg-4">
        <div class="col-12">
            <label>@_localizer["Settings_Gotify_Endpoint_Label"].Value</label>
            <input class="setting-field form-control" data-setting-name="@Notifications.Gotify.Endpoint" placeholder="https://gotify.your-domain.com/" value="@Model.GetValue(Notifications.Gotify.Endpoint.ToUpper(), string.Empty)" type="text" />
            <div class="form-text">@_localizer["Settings_Gotify_Endpoint_Help"].Value</div>
        </div>
    </div>

    <div class="row mb-4 mb-lg-4">
        <div class="col-12">
            <label>@_localizer["Settings_Gotify_Token_Label"].Value</label>
            <input class="setting-field form-control" data-setting-name="@Notifications.Gotify.Token" placeholder="my-token" value="@Model.GetValue(Notifications.Gotify.Token.ToUpper(), string.Empty)" type="password" />
            <div class="form-text">@_localizer["Settings_Gotify_Token_Help"].Value</div>
        </div>
    </div>

    <div class="row mb-4 mb-lg-4">
        <div class="col-12">
            <label>@_localizer["Settings_Gotify_Priority_Label"].Value</label>
            <input class="setting-field form-control" data-setting-name="@Notifications.Gotify.Priority" placeholder="4" value="@Model.GetValue(Notifications.Gotify.Priority.ToUpper(), "4")" type="number" min="1" max="10" step="1" />
            <div class="form-text">@_localizer["Settings_Gotify_Priority_Help"].Value</div>
        </div>
    </div>
}